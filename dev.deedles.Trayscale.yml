app-id: dev.deedles.Trayscale

runtime: org.gnome.Platform
runtime-version: '43'
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.golang

command: trayscale

finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri
  - --share=network
  - --share=ipc
  - --filesystem=/run/tailscale:ro
  - --talk-name=org.freedesktop.Notifications
  - --own-name=org.kde.*

build-options:
  prepend-path: /usr/lib/sdk/golang/bin
  #build-args:
  #  - --filesystem=home # Allows access to the Go build cache for easier local testing.

modules:
  - name: trayscale
    buildsystem: simple
    build-commands:
      - go build -v -trimpath -ldflags="-X 'deedles.dev/trayscale/internal/version.version=v0.8.4'" ./cmd/trayscale
      - install -Dm0755 trayscale $FLATPAK_DEST/bin/trayscale
      - install -Dm0644 dev.deedles.Trayscale.png $FLATPAK_DEST/share/icons/hicolor/256x256/apps/dev.deedles.Trayscale.png
      - install -Dm0644 dev.deedles.Trayscale.desktop $FLATPAK_DEST/share/applications/dev.deedles.Trayscale.desktop
      - install -Dm0644 dev.deedles.Trayscale.metainfo.xml $FLATPAK_DEST/share/metainfo/dev.deedles.Trayscale.metainfo.xml
    sources:
      - type: git
        url: https://github.com/DeedleFake/trayscale
        tag: v0.8.4
        commit: 50620ecd07d841ef0c779d163d5b92bc435d7338
      - type: archive
        url: https://github.com/DeedleFake/trayscale/releases/download/v0.8.4/trayscale-vendor.tar.gz
        sha256: 36225e1c858b7bf36b357a463579dab61625541b945aa60d5a458307cb7c8aa6
        strip-components: 0

  - name: tailscale
    buildsystem: simple
    build-commands:
      - go build -v -trimpath ./cmd/tailscale
      - install -Dm0755 tailscale $FLATPAK_DEST/bin/tailscale
    sources:
      - type: git
        url: https://github.com/tailscale/tailscale
        tag: v1.38.2
        commit: 3db61d07ca81aea7b86591d1874e48db01b6abcf
      - type: archive
        url: https://github.com/DeedleFake/trayscale/releases/download/v0.8.3/tailscale-vendor.tar.gz
        sha256: 1a6fc1333f4bd33804bdf8c026487132e88688fb22bbd97703ed4c087bbf9e34
        strip-components: 0
